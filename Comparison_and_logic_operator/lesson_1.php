<?php
$username = "admin";
$password = "secret";
$validLogin = ($username == "admin") && ($password == "secret");
// Don't change the line below
echo "Login valid: " . ($validLogin ? "true" : "false");
?>